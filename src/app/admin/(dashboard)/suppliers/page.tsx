import SuppliersClient from './SuppliersClient';

export const metadata = { title: 'Proveedores — Admin Rexermi' };
export const dynamic = 'force-dynamic';

export default function SuppliersPage() {
  return <SuppliersClient />;
}
